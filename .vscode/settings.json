{
    "files.exclude": {
        "**/node_modules": true,
        "**/*.js": { "when": "$(basename).ts" },
        "**/*.map": { "when": "$(basename)" }
    },
    "search.exclude": {
        "**/node_modules": true,
        "**/*.js": { "when": "$(basename).ts" },
        "**/*.map": { "when": "$(basename)" }
    }
}